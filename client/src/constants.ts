import { Range } from './types/math';

export const GRAVITY = 0.25;

export const TARGET_X_RANGE: Range = {
  min: 0.2,
  max: 0.8,
};
export const TARGET_Y_RANGE: Range = {
  min: 0.2,
  max: 0.5,
};